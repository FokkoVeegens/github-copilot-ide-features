{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Website",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8000/",
      "webRoot": "${workspaceFolder}/site",
      "preLaunchTask": "Run website locally"
    }
  ]
}